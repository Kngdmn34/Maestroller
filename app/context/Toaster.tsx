"use client"

import React from 'react'
import { Toaster } from 'react-hot-toast'

const ToasterNext = () => {
    return (
        <Toaster
            position="top-center"
            reverseOrder={false}
        />
    )
}

export default ToasterNext